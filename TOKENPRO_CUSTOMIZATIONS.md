# TokenPro 二次开发清单

当前分支以 Sub2API `v0.2.2` 为底层版本，并保留以下 TokenPro 功能。此文件用于后续升级时区分上游改动和本项目改动。

## 1. TokenPro 全局 API Key

- API Key 使用独立 `key_type` 区分 `global` 与普通 `group` 类型，不依赖名称判断。
- 系统全局 `TokenPro` Key 不可删除；用户可通过重置功能轮换 Key 值，重置保留原记录 ID 并强制 `group_id` 为空。
- 全局 Key 的 `group_id` 为空；普通 Key 继续固定绑定原分组。
- 注册及存量用户补齐全局 Key 的服务逻辑和幂等数据库迁移。
- 每个请求根据真实模型独立解析可用分组和账号，不把结果回写到 Key。
- OpenAI、Codex、Claude、Gemini、Grok 等入口接入全局 Key 动态路由，包括 HTTP、流式与 WebSocket 路径。
- 没有可用分组时返回“当前模型没有可用分组或有效订阅。”。
- API Key 鉴权缓存保留 Key 类型、模型白名单和 Codex manifest 配置，避免普通分组 Key 行为退化。

## 2. 分组与调度兼容

- 保留普通分组 Key 的创建、编辑、删除、固定分组调用行为。
- 全局 Key 动态选择时过滤禁用、过期、无账号或不可调度的分组。
- 使用实际命中的分组和账号进入后续调度、使用记录与计费链路。
- 合并 `v0.2.2` 新增的分组模型白名单；WebSocket 请求先解析全局 Key，再校验命中分组的白名单。
- 全局 Key 在用户列表中固定置底，标记为 TokenPro 自动调度专用；隐藏手动使用、复制、CCS 导入、启停和编辑入口，仅保留重置维护能力，后端也拒绝普通更新接口修改。
- 迁移 237 会幂等补齐曾被旧版本删除或遗漏的全局 Key，历史软删除记录保留用于审计。

## 3. 支付与计费二开

- 支付渠道配置、渠道限额、产品名迁移和商业配置迁移。
- 支付订单保存渠道快照，调整支付结果处理与后台管理接口。
- 固定手续费、渠道负载均衡、币种名称及充值倍率展示。
- 支付页、Stripe 支付页、结果页和相关回归测试。

## 4. 安全审计与内容监控

- 提示词审计配置、持久化、事件入队、处理和查询扩展。
- 内容监控后台页面、API、路由、菜单和中英文文案。
- 审计日志补充请求信息，并保留 TokenPro 的安全策略配置。

## 5. 用户与后台管理

- 记录用户注册 IP，并在后台用户列表展示。
- 后台用户、分组、设置和使用记录相关的 TokenPro 界面调整。
- 模型广场价格展示、排序与测试调整。

## 6. 品牌、文档与部署

- TokenPro 版本号显示规则及 Sub2API 底层版本映射。
- 管理员登录后检查底层版本；发现新版本时显示常驻提醒，同一浏览器会话对同一版本只提醒一次。
- Agent 使用文档页面、菜单、样式和中英文内容。
- 低内存 Docker 构建文件 `deploy/Dockerfile.lowmem`。

## 7. 数据库变更

- `196_add_user_created_ip.sql`：用户注册 IP。
- `234_api_key_scope.sql`：API Key 类型/作用域及全局 Key 补齐。
- Ent schema 和生成代码同步上述字段。

## 后续升级约定

1. 先比较上一个底层标签与新标签，再将 TokenPro 差异以三方方式应用到新标签。
2. 冲突优先同时保留上游修复和上述 TokenPro 行为，不以整文件覆盖。
3. 每次升级同步 `tokenProUpstreamVersion` 与 `backend/cmd/server/VERSION`。
4. 上线前至少执行后端编译/单元测试、前端类型检查/测试、数据库迁移检查，以及全局 Key 的并发、流式和普通分组 Key 回归测试。
